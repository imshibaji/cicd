name: demo workflow
on: push
jobs:
    demo-job:
        runs-on: macos-26
        steps:
            - name: Run a test script
              run: echo "Hello World"
            - uses: actions/checkout@v4
            - name: Install dependencies
              run: npm install
